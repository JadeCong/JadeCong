name: Gen Stargazers List - JadeCong

run-name: Gen Stargazers List - JadeCong

on:
  schedule:
    - cron: "0 5 * * 1,3,5" # Run every monday, wednesday and friday at 5:00 AM
  workflow_dispatch: # Manual dispatch

permissions:
  contents: write

env:
  GH_TOKEN: ${{ github.token }}

defaults:
  run:
    shell: bash
    working-directory: ./scripts

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  gen-stargazers-list:
    name: Gen Stargazers List
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out the repository to the runner
        uses: actions/checkout@v4
      
      - name: Configure git
        run: |
          git config --global user.name "github-actions[JadeCong-bot]"
          git config --global user.email "github-actions[JadeCong-bot]@users.noreply.github.com"
      
      - name: Run script gen-stargazers-list
        run: echo "JadeCong" | ./gen-stargazers-list.sh
        env:
          GH_TOKEN: ${{ github.token }}
      
      - name: Commit changes
        run: |
          cd ..
          git add .
          git commit -m "Update Stargazers List for JadeCong" || exit 0
      
      - name: Push changes
        run: git push
        env:
          GH_TOKEN: ${{ github.token }}
